<li class="message-item">
    {{#if (isJoinRequest type)}}
        <div>Group join request from {{senderName}} <button class="acceptJoinRequest" type="button" class="btn btn-light" data-messageId="{{id}}">Accept</button><button class="ignoreJoinRequest" type="button" class="btn btn-light" data-messageId="{{id}}">Ignore</button></div>
    {{/if}}
</li>